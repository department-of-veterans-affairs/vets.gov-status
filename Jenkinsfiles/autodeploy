pipeline {
  agent {
    label ''
  }

  stages {
    stage('Build') {
      steps {

    		withCredentials([file(credentialsId: 'ga_serviceaccount', variable: 'FILE')]) {
          		sh 'use $FILE'
        	}
      }
    }
  }
}
