pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
    		withCredentials([file(credentialsId: 'ga_serviceaccount', variable: 'GA_SERVICEACCOUNT')]) {
          		sh 'run_update.sh'
        	}
      }
    }
  }
}
